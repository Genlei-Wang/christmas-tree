# 部署指南 - GitHub Pages & Vercel

## 🚀 方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **Vercel** | ⚡ 最快最简单，支持拖拽部署 | 需要注册账号 | ⭐⭐⭐⭐⭐ |
| **GitHub Pages** | 免费，与GitHub集成好 | 需要Git仓库，设置稍复杂 | ⭐⭐⭐⭐ |
| **Netlify** | 简单，功能强大 | 需要注册账号 | ⭐⭐⭐⭐ |

## 📦 方案1：Vercel（推荐，最快）

### 方式A：通过网页拖拽部署（最简单）

1. **访问 Vercel**
   - 打开 https://vercel.com
   - 使用 GitHub 账号登录（推荐）

2. **部署项目**
   - 点击 "Add New..." → "Project"
   - 选择 "Upload" 标签
   - 将整个项目文件夹拖拽到上传区域
   - 点击 "Deploy"

3. **获得网址**
   - 部署完成后，会显示网址（例如：`https://christmas-tree-xxx.vercel.app`）
   - 访问：`https://your-site.vercel.app/ChristmasTree.html`
   - 访问：`https://your-site.vercel.app/ChristmasTree-sphere.html`

4. **分享给同事**
   - 直接发送网址即可！

### 方式B：通过GitHub仓库部署（推荐，自动更新）

1. **创建GitHub仓库**
   ```bash
   # 在项目目录下
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/你的用户名/christmas-tree.git
   git push -u origin main
   ```

2. **在Vercel连接GitHub**
   - 访问 https://vercel.com
   - 点击 "Add New..." → "Project"
   - 选择 "Import Git Repository"
   - 选择你的 `christmas-tree` 仓库
   - 点击 "Import"

3. **配置部署**
   - Framework Preset: 选择 "Other" 或留空
   - Root Directory: `./`（默认）
   - Build Command: 留空（静态文件，无需构建）
   - Output Directory: 留空
   - 点击 "Deploy"

4. **自动部署**
   - 每次推送到GitHub，Vercel会自动重新部署
   - 网址格式：`https://christmas-tree-你的用户名.vercel.app`

## 📦 方案2：GitHub Pages（免费，稳定）

### 步骤1：创建GitHub仓库

```bash
# 在项目目录下执行
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的用户名/christmas-tree.git
git push -u origin main
```

### 步骤2：启用GitHub Pages

1. 在GitHub仓库页面，点击 **Settings** 标签
2. 左侧菜单找到 **Pages**
3. 在 "Source" 下选择：
   - Branch: `main`
   - Folder: `/ (root)`
4. 点击 **Save**

### 步骤3：访问网址

- 等待1-2分钟部署完成
- 访问：`https://你的用户名.github.io/christmas-tree/ChristmasTree.html`
- 访问：`https://你的用户名.github.io/christmas-tree/ChristmasTree-sphere.html`

### 步骤4：自定义域名（可选）

如果想使用自己的域名：
1. 在GitHub Pages设置中添加自定义域名
2. 在DNS中添加CNAME记录指向 `你的用户名.github.io`

## 🔧 方案3：Vercel CLI（高级用户）

如果你喜欢命令行：

```bash
# 安装Vercel CLI
npm i -g vercel

# 在项目目录下
vercel

# 按照提示操作：
# - 登录账号
# - 选择项目设置
# - 部署完成
```

## 📝 推荐配置

### 创建 `.vercelignore`（可选）

如果使用Vercel，可以创建此文件忽略不需要的文件：

```
解决方案说明.md
部署指南.md
README.md
备份.md
创建圣诞树的详细方案.md
.cursor/
```

### 创建 `vercel.json`（可选）

如果需要自定义配置：

```json
{
  "version": 2,
  "builds": [
    {
      "src": "*.html",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/ChristmasTree.html",
      "dest": "/ChristmasTree.html"
    },
    {
      "src": "/ChristmasTree-sphere.html",
      "dest": "/ChristmasTree-sphere.html"
    }
  ]
}
```

## 🎯 快速选择

**如果你想要：**
- ⚡ **最快部署** → 选择 Vercel 拖拽部署
- 🔄 **自动更新** → 选择 Vercel + GitHub 集成
- 💰 **完全免费** → 选择 GitHub Pages
- 🎨 **自定义域名** → 选择 GitHub Pages 或 Vercel

## 📱 分享链接格式

部署完成后，你的链接格式：

**Vercel:**
- `https://christmas-tree-xxx.vercel.app/ChristmasTree.html`
- `https://christmas-tree-xxx.vercel.app/ChristmasTree-sphere.html`

**GitHub Pages:**
- `https://你的用户名.github.io/christmas-tree/ChristmasTree.html`
- `https://你的用户名.github.io/christmas-tree/ChristmasTree-sphere.html`

## ✅ 部署检查清单

- [ ] 文件已上传/推送到仓库
- [ ] 部署成功（无错误）
- [ ] 可以访问HTML文件
- [ ] Three.js等资源正常加载
- [ ] 手势识别功能正常（需要摄像头权限）
- [ ] 音乐播放正常

## 🐛 常见问题

**Q: Vercel部署后显示404？**
A: 确保访问完整路径，包括文件名：`https://your-site.vercel.app/ChristmasTree.html`

**Q: GitHub Pages显示404？**
A: 检查仓库设置中的Pages配置，确保选择了正确的分支和目录。

**Q: 资源加载失败？**
A: 确保使用HTTPS访问（Vercel和GitHub Pages都默认使用HTTPS）。

## 🎉 完成！

部署完成后，直接发送网址给同事即可，无需任何额外设置！

